root = true

[*]
insert_final_newline = true

[*.{kt,kts}]
# Compose の @Composable 関数は大文字始まりを許可
ktlint_function_naming_ignore_when_annotated_with = Composable
# Compose で頻出するワイルドカード import を許可
ktlint_standard_no-wildcard-imports = disabled
